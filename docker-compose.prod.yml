name: alive-prod

x-backend: &backend
  platform: ${DEPLOY_PLATFORM:-linux/amd64}
  build:
    context: ./backend
    dockerfile: Dockerfile
  image: alive-backend:${BACKEND_IMAGE_TAG:-latest}
  env_file:
    - ${PROD_ENV_FILE:-.env.prod}
  networks:
    - level-up

services:
  db-bootstrap:
    platform: ${DEPLOY_PLATFORM:-linux/amd64}
    image: postgres:16-alpine
    profiles:
      - db-bootstrap
    restart: "no"
    env_file:
      - ${DB_BOOTSTRAP_ENV_FILE:-.env.db-bootstrap}
    networks:
      - level-up
    command:
      - /bin/sh
      - -ec
      - |
        : "$${PGHOST:?PGHOST is required}"
        : "$${PGUSER:?PGUSER is required}"
        : "$${PGPASSWORD:?PGPASSWORD is required}"
        : "$${APP_DB_NAME:?APP_DB_NAME is required}"
        : "$${APP_DB_USER:?APP_DB_USER is required}"
        : "$${APP_DB_PASSWORD:?APP_DB_PASSWORD is required}"
        psql --set=ON_ERROR_STOP=1 \
          --set=app_db="$${APP_DB_NAME}" \
          --set=app_user="$${APP_DB_USER}" \
          --set=app_password="$${APP_DB_PASSWORD}" <<'SQL'
        SELECT format('CREATE ROLE %I LOGIN PASSWORD %L', :'app_user', :'app_password')
        WHERE NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = :'app_user')
        \gexec
        SELECT format('CREATE DATABASE %I OWNER %I', :'app_db', :'app_user')
        WHERE NOT EXISTS (SELECT 1 FROM pg_database WHERE datname = :'app_db')
        \gexec
        SELECT format('GRANT ALL PRIVILEGES ON DATABASE %I TO %I', :'app_db', :'app_user')
        \gexec
        SQL
        psql --dbname="$${APP_DB_NAME}" \
          --set=ON_ERROR_STOP=1 \
          --set=app_user="$${APP_DB_USER}" <<'SQL'
        SELECT format('GRANT ALL ON SCHEMA public TO %I', :'app_user')
        \gexec
        SQL

  migrate:
    <<: *backend
    restart: "no"
    networks:
      - level-up
    command:
      - alembic
      - upgrade
      - head

  backend:
    <<: *backend
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-8888}:8888"
    networks:
    - level-up
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - >-
          import urllib.request;
          urllib.request.urlopen("http://127.0.0.1:8888/health", timeout=3)
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

networks:
  database:
    external: true
    name: ${DB_DOCKER_NETWORK:?DB_DOCKER_NETWORK is required}
