# Frontend-local guidance

For React/Vite component placement, state ownership, folder structure, and UI architecture, use `.agents/skills/alive-frontend-architecture/SKILL.md` and load only the relevant reference.

Keep frontend changes feature-local, preserve the existing `api`/`domain`/`hooks`/`features` boundaries, and do not introduce a new state library or UI dependency without a focused comparison and verification plan.
